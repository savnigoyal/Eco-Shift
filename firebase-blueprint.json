{
  "entities": {
    "CarbonInput": {
      "title": "Carbon Input",
      "description": "User's recorded carbon usage factors.",
      "type": "object",
      "properties": {
        "transport_mode": {
          "type": "string",
          "enum": ["car", "bus", "metro", "bike", "cycle"],
          "description": "The primary daily transport vehicle category used."
        },
        "transport_distance": {
          "type": "integer",
          "description": "Approximate daily distance traveled in kilometers."
        },
        "ac_hours": {
          "type": "integer",
          "description": "Number of hours of air conditioning use per day."
        },
        "laptop_hours": {
          "type": "integer",
          "description": "Number of hours spent using a laptop per day."
        },
        "waste_habit": {
          "type": "string",
          "enum": ["high_waste", "mixed_recycler", "zero_waste"],
          "description": "User's waste production and recycling habit style."
        },
        "digital_hours": {
          "type": "integer",
          "description": "Daily screen and digital consumption hours."
        }
      },
      "required": [
        "transport_mode",
        "transport_distance",
        "ac_hours",
        "laptop_hours",
        "waste_habit",
        "digital_hours"
      ]
    }
  },
  "firestore": {
    "/carbon_inputs/{userId}": {
      "schema": "CarbonInput",
      "description": "Location of a single user's daily logged carbon habits."
    }
  }
}
